'use client';

import Lottie from 'lottie-react';
import androidNewVersionAnimation from '/public/lottie/android-new-version.json';

const AndroidNewVersionAnimation = () => {
  return (
    <Lottie animationData={androidNewVersionAnimation} />
  );
};

export default AndroidNewVersionAnimation;
